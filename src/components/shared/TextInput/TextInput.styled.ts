import styled from "styled-components/macro";
import { COLOR } from "../../../styles/color.styled";

const StyledInput = styled.input`
  padding: 0.5rem 0.5rem;
  border: 0.0625rem solid ${COLOR.WildBlueYonder};
  border-radius: 0.25rem;
`;

const S = { StyledInput };

export default S;
