import styled from "styled-components/macro";

const Container = styled.div`
  position: fixed;
  z-index: 100;
  top: 40px;
  left: 50%;
  transform: translateX(-50%);
`;

const S = { Container };

export default S;
